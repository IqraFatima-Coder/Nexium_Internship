import { RequirementsVerifier } from "@/components/requirements-verifier";

export default function VerifyRequirementsPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background via-muted/20 to-background">
      <RequirementsVerifier />
    </div>
  );
}
